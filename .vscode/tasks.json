{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build",
            "type": "shell",
            "command": "odin build animagik -debug -out:build/animagik-dbg -extra-linker-flags:\"-Lbuild/libs -Wl,-rpath=build/libs\"",
            "group": "build"
        }
    ]
}
